export interface BasicServiceType {
    userId: number;
    service: string;
    price: string;
   // description: string;
}
export interface UpdateServiceDataType {
    id: string;
    service: string;
    price: string;
}